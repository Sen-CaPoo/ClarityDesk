@page
@model ClarityDesk.Pages.Account.LoginModel
@{
    ViewData["Title"] = "登入";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card mt-5">
                <div class="card-body text-center p-5">
                    <h2 class="card-title mb-4">歡迎使用 ClarityDesk</h2>
                    <p class="text-muted mb-4">顧客問題紀錄追蹤系統</p>
                    
                    <div class="mb-4">
                        <i class="bi bi-person-circle" style="font-size: 4rem; color: var(--primary-color);"></i>
                    </div>

                    <form method="post">
                        <button type="submit" class="btn btn-success btn-lg w-100">
                            <i class="bi bi-line me-2"></i>
                            使用 LINE 登入
                        </button>
                    </form>

                    <div class="divider my-4">
                        <span class="divider-text">或</span>
                    </div>

                    <form method="post" asp-page-handler="Guest">
                        <button type="submit" class="btn btn-outline-secondary btn-lg w-100">
                            <i class="bi bi-person me-2"></i>
                            以訪客身份繼續
                        </button>
                    </form>

                    <p class="text-muted small mt-4 mb-0">
                        透過 LINE 帳號快速登入，首次登入將自動建立帳號<br>
                        或選擇訪客模式以一般使用者身份快速體驗
                    </p>
                </div>
            </div>

            <div class="text-center mt-3">
                <small class="text-muted">
                    登入即表示您同意我們的服務條款與隱私政策
                </small>
            </div>
        </div>
    </div>
</div>

<style>
    .card {
        background: var(--card-background);
        border-radius: var(--border-radius);
        box-shadow: var(--box-shadow);
        border: none;
    }

    .btn-success {
        background-color: #06C755;
        border-color: #06C755;
        font-weight: 600;
    }

    .btn-success:hover {
        background-color: #05b04c;
        border-color: #05b04c;
    }

    .divider {
        display: flex;
        align-items: center;
        text-align: center;
    }

    .divider::before,
    .divider::after {
        content: '';
        flex: 1;
        border-bottom: 1px solid #dee2e6;
    }

    .divider-text {
        padding: 0 10px;
        color: #6c757d;
        font-size: 0.875rem;
    }

    body {
        background-color: #f5f7fa;
    }
</style>
